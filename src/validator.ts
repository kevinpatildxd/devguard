import { ParsedEnv, ValidationResult } from './types';

export function validate(_env: ParsedEnv, _example: ParsedEnv): ValidationResult[] {
  // Rules will be wired in here during Phase 2
  return [];
}
